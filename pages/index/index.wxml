<view>
  <menu-button background="{{item.bg}}" colorClass="{{item.color}}" url="{{item.url}}" icon="/images/logo.png" title="{{item.name}}" />
  <menu-button background="{{jeu.bg}}" colorClass="{{jeu.color}}" url="{{jeu.url}}" icon="/images/logo.png" title="{{jeu.name}}" />
</view>